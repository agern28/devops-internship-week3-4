# Day 17 — EXAMPLE ONLY. This file is committed; the real Secret is not.
# Create it imperatively instead:
#   kubectl create secret generic app-secret \
#     --from-literal=API_KEY='...' -n app
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: app
type: Opaque
stringData:
  API_KEY: "REPLACE_ME"
